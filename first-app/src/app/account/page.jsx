export const metadata = {
    title: "Account"
}

const Account = () => {
    return (
        <div>
            <h1>Account Page</h1>
            <a href="/account/profile">Profile Page</a>
        </div>
    );
}

export default Account;